DirectoryIndex index.html

RewriteEngine On

# Redirect any request for an explicit "index.html" (root or subfolder)
# to the containing folder, so the address bar never shows index.html.
RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\s(/(?:[^\s/]+/)*)index\.html[\s?] [NC]
RewriteRule ^ %1 [R=301,L]
